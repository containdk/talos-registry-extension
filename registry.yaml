name: registry
depends:
  - service: cri
  - network:
      - addresses
      - connectivity
      - etcfiles
  - configuration: true
  - path: /var/mnt/zot-registry
container:
  entrypoint: /bin/zot
  args:
    - serve
    - /etc/zot/config.json
  security:
    writeableRootfs: false
    writeableSysfs: true
  mounts:
    - destination: /var/lib/registry
      type: bind
      source: /var/mnt/zot-registry
      options:
        - rbind
        - rshared
        - rw
restart: always
